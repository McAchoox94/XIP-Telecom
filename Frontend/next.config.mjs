/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["avatar.vercel.sh"], // Autorise l'affichage des images depuis cet hôte
    },
};

export default nextConfig;